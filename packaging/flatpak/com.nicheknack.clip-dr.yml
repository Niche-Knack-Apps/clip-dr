app-id: com.nicheknack.clip-dr
runtime: org.gnome.Platform
runtime-version: '48'
sdk: org.gnome.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
  - org.freedesktop.Sdk.Extension.node22

command: clip-dr

finish-args:
  # Display access
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland

  # Audio access (PulseAudio/PipeWire)
  - --socket=pulseaudio

  # File access for opening/saving audio files
  - --filesystem=home
  - --filesystem=xdg-documents
  - --filesystem=xdg-music
  - --filesystem=xdg-download

  # GPU acceleration
  - --device=dri

  # Network access (for downloading whisper models on first run)
  - --share=network

  # Intel GPU workaround - fixes EGL initialization on Alder Lake+ iGPUs
  - --env=WEBKIT_DISABLE_DMABUF_RENDERER=1

build-options:
  append-path: /usr/lib/sdk/rust-stable/bin:/usr/lib/sdk/node22/bin
  env:
    CARGO_HOME: /run/build/clip-dr/cargo
    npm_config_nodedir: /usr/lib/sdk/node22

modules:
  - name: clip-dr
    buildsystem: simple
    build-options:
      env:
        CARGO_HOME: /run/build/clip-dr/cargo
        HOME: /run/build/clip-dr
    build-commands:
      # Install npm dependencies (use install instead of ci for sandbox compatibility)
      - HOME=/run/build/clip-dr npm install --legacy-peer-deps

      # Build frontend
      - HOME=/run/build/clip-dr npm run build

      # Build Rust backend
      - cd src-tauri && cargo build --release

      # Install binary
      - install -Dm755 src-tauri/target/release/clip-dr /app/bin/clip-dr

      # Install icons
      - install -Dm644 src-tauri/icons/icon.png /app/share/icons/hicolor/512x512/apps/com.nicheknack.clip-dr.png
      - install -Dm644 src-tauri/icons/128x128.png /app/share/icons/hicolor/128x128/apps/com.nicheknack.clip-dr.png
      - install -Dm644 src-tauri/icons/32x32.png /app/share/icons/hicolor/32x32/apps/com.nicheknack.clip-dr.png

      # Install desktop file
      - install -Dm644 packaging/flatpak/com.nicheknack.clip-dr.desktop /app/share/applications/com.nicheknack.clip-dr.desktop

      # Install metainfo
      - install -Dm644 packaging/flatpak/com.nicheknack.clip-dr.metainfo.xml /app/share/metainfo/com.nicheknack.clip-dr.metainfo.xml

    sources:
      - type: dir
        path: ../..
